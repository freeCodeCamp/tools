const catchAll = require('./catchAll');
const pareto = require('./pareto');
const pr = require('./pr');
const search = require('./search');
const info = require('./info');
const getCurrData = require('./getCurrData');
const updateData = require('./update-data');
const auth = require('./auth');
const user = require('./user');

module.exports = { catchAll, pareto, pr, search, info, getCurrData, updateData, auth, user };
